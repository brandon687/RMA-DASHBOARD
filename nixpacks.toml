[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm ci --omit=dev"]

[phases.build]
cmds = ["echo 'No build step required - Express server'"]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"
